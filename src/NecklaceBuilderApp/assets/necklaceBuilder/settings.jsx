import chainTypes from "./elements/ChainTypes"
import beadTypes from "./elements/BeadTypes"

const settings = {
    chainTypes:chainTypes,
    beadTypes:beadTypes,
    length:{ min:1, max:34 }
}
export default settings